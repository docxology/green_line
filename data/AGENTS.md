# data/AGENTS.md

Generated files (`formalism_claim_ledger.json`, `envelopes/*.json`) are
regenerated by their scripts; hand edits fail the ledger tests. The binding
declaration is prepared input for line_set — never write into line_set from
here. Nothing in this directory may contain a number the code can derive
without a test re-deriving it.
