# Conclusion

The Green Line holds a place the other lines deliberately do not: the record
of what is still growing, held open on purpose, without opus stage and
without completion claims. Its failure modes are the genres it must never
become, and its counter-signal intake makes those failure modes visible as
drift rather than scored as content. As a set member it is ordinary: one
registry, one evaluator, one envelope schema, no special cases.
