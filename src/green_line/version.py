"""Version markers for the Green Line package.

The package version tracks the code surface; the registry digest (see
``serialization.registry_digest``) tracks the growth-record content.
Reviewers can pin both when citing a verdict.
"""

__version__ = "0.1.0"
